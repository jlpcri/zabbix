---
# For the Zabbix monitoring, the default is to use
# the production server and over-ride per subnet:
zabbix:
  server: "{{ zabbix_prod }}"
  owner: "{{ zabbix_eod }}"
  group: "{{ zabbix_eod }}"

...
